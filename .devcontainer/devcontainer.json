{
  "name": "Swiss",
  "build": {
    "dockerfile": "../Dockerfile",
    "args": {
      "GIT_USER_EMAIL": "${localEnv:GIT_USER_EMAIL}",
      "GIT_USER_NAME": "${localEnv:GIT_USER_NAME}"
    }
  },
  "features": {
    "ghcr.io/devcontainers/features/common-utils:2.5.2": {
      "username": "vscode"
    },
    "ghcr.io/devcontainers/features/docker-in-docker:2": {},
    "ghcr.io/dhoeric/features/google-cloud-cli:1": {},
    "ghcr.io/devcontainers/features/github-cli:1": {}
  },
  "postStartCommand": [
    "/bin/sh",
    "-c",
    "git --version && poetry config virtualenvs.in-project true && poetry install && git init && git config --global user.email \"${GIT_USER_EMAIL}\" && git config --global user.name \"${GIT_USER_NAME}\" && poetry run pre-commit install --install-hooks"
  ],
  "customizations": {
    "vscode": {
      "settings": {
        "python.linting.mypyEnabled": true,
        "python.linting.enabled": true,
        "python.analysis.typeCheckingMode": "strict",
        "python.analysis.diagnosticMode": "workspace",
        "editor.formatOnSave": true,
        "python.languageServer": "Pylance",
        "python.linting.pylintEnabled": true,
        "python.formatting.provider": "black",
        "editor.codeActionsOnSave": {
          "source.organizeImports": true,
          "source.fixAll": true
        }
      },
      "extensions": [
        "ms-python.python",
        "ms-python.vscode-pylance",
        "ms-python.mypy-type-checker",
        "ms-python.isort",
        "ms-python.pylint",
        "charliermarsh.ruff",
        "tamasfe.even-better-toml"
      ]
    }
  },
  "containerUser": "vscode"
}